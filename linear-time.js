//O(n) - linear time 
//Two arrays with the same indentation
function compressBoxesTwice (boxes1, boxes2){
  boxes1.forEach (function(boxes1){
    CONSOLE.LOG(boxes1);
  });
  
  boxes2.forEach (function(boxes2){
    CONSOLE.LOG(boxes2);
  });
}